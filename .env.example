# xAI (Grok) API key — required for --prompt / createFlowchartFromPrompt
# Get a key at https://console.x.ai (ensure it has Chat Completions access)
# Copy this file to .env and set your key (do not commit .env)
XAI_API_KEY=

# Optional: model name (default: grok-3-latest). Must match a model enabled for your key in console.x.ai.
# XAI_MODEL=grok-2-1212
